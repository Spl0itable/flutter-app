<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Core connectivity -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Geolocation for proximity channel sort (geolocator). Coarse + fine; the
         fix is only read when the user enables "Sort by proximity". -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- WebRTC audio/video calls + screenshare (flutter_webrtc),
         camera (image_picker/mobile_scanner), microphone (calls/audio) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- WebRTC routes audio through Bluetooth headsets; new + legacy names -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- Bluetooth LE mesh (bitchat-compatible): scan for and advertise the mesh
         GATT service, and act as both central and peripheral. SCAN declares
         neverForLocation since mesh discovery filters by service UUID and does
         not derive device location. ADMIN/legacy names cover pre-Android-12. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false"/>

    <!-- Keep the connection/call alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Foreground service for active calls + screen-capture (getDisplayMedia) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <!-- "Stay Connected in Background": relay sockets (dataSync) and the BLE
         mesh radio (connectedDevice) keep running while the app is off-screen.
         Only used while that opt-in setting is on. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>

    <!-- Notifications (local + future FCM). POST_NOTIFICATIONS needed on API 33+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Exact-alarm-free scheduling; receive boot for rescheduled notifications -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Biometric / device-credential unlock (local_auth) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <!-- Media access for image_picker / file_picker / gal (scoped storage).
         API 33+ uses granular READ_MEDIA_*; legacy <=32 uses READ_EXTERNAL_STORAGE. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- gal: saving images/videos to the gallery on legacy API levels -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>

    <!-- Hardware features are optional so the app installs on devices lacking them -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <!-- Backups are fully disabled: Auto Backup would copy the sqflite message
         store and SharedPreferences (message history, group membership,
         contacts) to the user's Google account, and adb backup could extract
         the same data locally. allowBackup=false disables both; the rule files
         are belt-and-braces for agents that consult rules despite the flag.
         Identity secrets already live in the Keystore. -->
    <application
        android:label="Nymchat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom scheme: nymchat://… (app_links / deep_links) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="nymchat"/>
            </intent-filter>
            <!-- Universal links: the hosts the deep-link parser recognizes
                 (deep_links.dart kNymLinkHosts) + the served PWA host.
                 autoVerify drives App Links against /.well-known/assetlinks.json.

                 Scoped to the ROOT PATH on purpose. Every link the parser
                 understands routes on the URL fragment (`/#u4pruyd`,
                 `/#gjoin=...`, `/#e:...` — parseNymLink returns null without
                 one), so their path is always "/" and none of them lose their
                 App Link. What this DOES free is every other path on these
                 hosts: `/shop/...`, which the app opens itself to hand a buyer
                 off to the web for an external Lightning purchase, and the
                 legal pages under `/terms/` and `/privacy/`. Claiming those
                 made the app resolve its own outbound links straight back into
                 itself instead of a browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="app.nymchat.app" android:path="/"/>
                <data android:scheme="https" android:host="app.nym.bar" android:path="/"/>
                <data android:scheme="https" android:host="web.nymchat.app" android:path="/"/>
                <data android:scheme="https" android:host="nymchat.app" android:path="/"/>
            </intent-filter>
            <!-- Share sheet: receive text/URLs shared from other apps
                 (receive_sharing_intent). The plugin reads the SEND intent and
                 delivers it to Dart, which shows the destination picker. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/*"/>
            </intent-filter>
            <!-- Share sheet: receive images/videos (single + multiple). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
                <data android:mimeType="video/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
                <data android:mimeType="video/*"/>
            </intent-filter>
        </activity>
        <!-- "Stay Connected in Background" keep-alive. Not exported: only the
             app itself starts or stops it. -->
        <service
            android:name=".NymBackgroundService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="dataSync|connectedDevice"/>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open a Lightning wallet via lightning: URIs (zaps/shop) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="lightning"/>
        </intent>
        <!-- url_launcher: open external https links + share targets -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="*/*"/>
        </intent>
    </queries>
</manifest>
